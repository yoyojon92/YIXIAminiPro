# 邑夏小程序 - 首页去掉"宿舍1元达"配送提示条

## ⚠️ 铁律：不要用AI生成任何图片！不要修改/删除已有图片文件！只改代码！

---

## 任务目标

首页顶部有一个"宿舍1元达 送货到宿舍，便捷快速"的配送提示条（深蓝底色+白色文字+右侧按钮），需要整个删除。

---

## 修改文件：src/pages/index/index.tsx

搜索首页中包含"宿舍1元达""送货到宿舍""1元达"相关文字的 `<View>` 区块，**整个删除**。

典型代码特征：
- 文字"宿舍1元达"或"送货到宿舍"
- 深蓝色背景容器
- 右侧有"立即"或箭头按钮

删除整个容器View及其内容，不留空行。

---

## 验证

```bash
grep -rn "1元达\|宿舍.*配送\|送货到宿舍" src/ --include="*.tsx" --include="*.ts"
# 必须=0

pnpm build:weapp
git add -A && git commit -m "fix: 首页去掉宿舍1元达配送提示条" && git push
```

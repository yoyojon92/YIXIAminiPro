# 邑夏小程序 - 去掉"邑夏精灵"板块

## ⚠️ 铁律：不要用AI生成任何图片！不要修改/删除已有图片文件！只改代码！

---

## 任务目标

从所有页面中**删除"邑夏精灵"相关板块和数据**，只保留"藏府君"相关内容。

---

## 修改文件1：src/pages/index/index.tsx

搜索首页中包含"邑夏精灵""精灵""sprite"相关区块的JSX代码，将整个"邑夏精灵"展示区域删除。

典型代码特征：
- 标题为"邑夏精灵"的 `<View>` 区块
- 包含欣欣、霏霏、番番等角色头像的展示区
- `spriteList` / `sprites` / `精灵` 相关的 map 渲染

**删除整个"邑夏精灵"区块的JSX，保留"藏府君"区块不动。**

---

## 修改文件2：src/mock/products.ts

在产品数据中，搜索所有果酒产品的 `spriteId` 和 `spriteAlias` 字段，将它们清空：

将以下6款果酒的精灵相关字段改为空值：
```
spriteId: ''
spriteAlias: ''
```

受影响的产品ID：
- prod_pomegranate_new（榴红心事）
- prod_grape_wine（葡香暗度）
- prod_peach_new（桃心暗动）
- prod_apple_wine（青苹微醺）
- prod_guava_wine（粉乐雾琼）
- prod_red_wine（干红葡萄酒）

---

## 修改文件3：其他页面

搜索所有 .tsx/.ts 文件中出现"邑夏精灵"文字的地方，删除对应展示区块或文字：
```bash
grep -rn "邑夏精灵\|精灵" src/ --include="*.tsx" --include="*.ts"
```

所有"邑夏精灵"标题/区块删除，但"藏府君"相关内容保留不动。

注意：`spriteId`/`spriteAlias` 字段定义在 Product interface 中不要删，只是值置空即可。

---

## 验证

```bash
# 确认没有"邑夏精灵"文字残留
grep -rn "邑夏精灵" src/ --include="*.ts" --include="*.tsx"
# 必须=0

# 确认"藏府君"还保留
grep -rn "藏府君" src/ --include="*.ts" --include="*.tsx"
# 应该>0

# 确认产品精灵字段已清空
grep "spriteAlias" src/mock/products.ts | grep -v "^.*spriteAlias: ''"
# 应该=0（所有spriteAlias都是空字符串）

pnpm build:weapp
git add -A && git commit -m "fix: 去掉邑夏精灵板块，保留藏府君" && git push
```

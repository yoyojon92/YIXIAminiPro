# 邑夏小程序 - 紧急：删除下架产品指令

## ⚠️ 铁律：图片文件和图片映射不要动！只删产品数据！

## 任务：从 src/mock/products.ts 的 MOCK_PRODUCTS 数组中，删除以下10个完整产品对象

每个产品对象从 `{` 开始到对应的 `},` 结束（包含注释行如 `// ============ 果酒系列`），整段删除。

### 要删除的10个产品（按顺序）：

1. **prod_peach_001**（桃你欢心）— 搜索 `id: 'prod_peach_001'` 删除整个 `{...},`
2. **prod_hawthorn_001**（楂香四溢）— 搜索 `id: 'prod_hawthorn_001'` 删除整个 `{...},`
3. **prod_pear_001**（大吉大梨）— 搜索 `id: 'prod_pear_001'` 删除整个 `{...},`
4. **prod_pomegranate_001**（似水榴年）— 搜索 `id: 'prod_pomegranate_001'` 删除整个 `{...},`
5. **prod_grape_001**（葡香暗度）— 搜索 `id: 'prod_grape_001'` 删除整个 `{...},`
6. **prod_xingshui_pinjian**（兴水河·品鉴）— 搜索 `id: 'prod_xingshui_pinjian'` 删除整个 `{...},`
7. **prod_xingshui_jingdian**（兴水河·经典）— 搜索 `id: 'prod_xingshui_jingdian'` 删除整个 `{...},`
8. **prod_xingshui_xiyan**（兴水河·禧宴）— 搜索 `id: 'prod_xingshui_xiyan'` 删除整个 `{...},`
9. **prod_mixed_001**（为爱而生）— 搜索 `id: 'prod_mixed_001'` 删除整个 `{...},`
10. **prod_peach_new**（桃心暗动）— 搜索 `id: 'prod_peach_new'` 删除整个 `{...},`

### 同时删除对应分类注释行：
- `// ============ 果酒系列（邑夏品牌，5款）============` → 改为 `// ============ 果酒系列（邑夏品牌，3款）============`
- `// ============ 粮食酒系列（兴水河品牌，3款）============` → 整行删除（3款都删了）
- `// ============ NFC果汁系列（果粟盈品牌，3款）============` → 改为 `// ============ 原果汁系列（果粟盈品牌，3款）============`

### 同时删除分类相关：
- MOCK_CATEGORIES 中删除 `{ id: 'cat_grain_wine', name: '露酒系列', ... }` 整个对象
- UNIFIED_CATEGORIES 中删除 `{ id: 'grain_wine', name: '露酒系列', ... }` 整个对象
- UnifiedCategoryId 类型中删除 `'grain_wine'`

### 拼团更新：
MOCK_FLASH_SALE 只保留2款：
```typescript
export const MOCK_FLASH_SALE: FlashSaleProduct[] = [
  {
    id: 'prod_pomegranate_new',
    name: '榴红心事',
    price: 28.8,
    pintuanPrice: 18.8,
    pintuanCount: 128,
    endTime: '2026-12-31 18:00:00',
    image: PRODUCT_IMAGES.prod_pomegranate_new,
    spriteAlias: '甘甘'
  },
  {
    id: 'prod_apple_001',
    name: '清苹微醉',
    price: 28.8,
    pintuanPrice: 18.8,
    pintuanCount: 96,
    endTime: '2026-12-31 18:00:00',
    image: PRODUCT_IMAGES.prod_apple_001,
    spriteAlias: '霏霏'
  }
]
```

### 删除后MOCK_PRODUCTS应该只剩7个产品：
1. prod_pomegranate_new（榴红心事）¥18.8
2. prod_apple_001（清苹微醉）¥18.8
3. prod_guava_001（番红暗许）¥18.8
4. prod_nfc_peach_001（鲜桃果汁）¥18.9
5. prod_nfc_grape_001（红葡萄果汁）¥18.9
6. prod_nfc_pear_001（鲜梨果汁）¥18.9
7. prod_gift_box_001（缤纷礼盒）¥68.8

## 其他文件同步：
- 搜索所有ts/tsx文件，删除涉及 grain_wine 分类tab/筛选的代码
- 搜索 "粮食酒" 替换为 "露酒"，"NFC果汁" 替换为 "原果汁"

## 验证：
```bash
# 确认10个被删产品不存在
grep -c "prod_peach_001\|prod_hawthorn_001\|prod_pear_001\|prod_pomegranate_001\|prod_grape_001\|prod_mixed_001\|prod_peach_new\|prod_xingshui" src/mock/products.ts
# 应该返回 0

# 确认只剩7个产品
grep -c "id: 'prod_" src/mock/products.ts
# 应该返回 7

# 确认grain_wine不存在
grep -rn "grain_wine\|粮食酒" src/ --include="*.ts" --include="*.tsx"

pnpm validate && pnpm build:weapp
git add -A && git commit -m "fix: 删除10款下架产品，精简至7款" && git push
```

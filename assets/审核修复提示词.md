---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 7629647366345376000-data_volume/files/所有对话/主对话/审核修复提示词.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 4496376016153680#1780116822161
    ReservedCode2: ""
---
# 邑夏小程序 - 审核前修复提示词（喂给Coze编程Agent）

请按以下顺序操作，**先验证每个问题是否确实存在，再进行修复**。每步验证时输出「✅ 已修复」或「❌ 确认存在，需要修复」。

## 1. 果酒混合礼盒原价问题

**验证**：打开 `src/mock/products.ts`，搜索 `gift_wine_box`，检查 `originalPrice` 字段。
- originalPrice=75.2 → ❌ 确认存在（售价79，原价低于售价）
- originalPrice>79 → ✅

**修复**：将 `gift_wine_box` 的 `originalPrice: 75.2` 改为 `originalPrice: 119.6`，同时将 specs 里 `originalPrice: 75.2` 也改为 `119.6`。

## 2. 隐私政策入口缺失

**验证**：打开 `src/pages/profile/index.tsx`，搜索 `隐私政策`。
- 无"隐私政策" → ❌ 确认存在
- 有 → ✅

**修复**：在 `toolItems` 数组末尾添加：

```typescript
{ id: 6, icon: Shield, title: '隐私政策', badge: null, action: 'privacy' }
```

修改 toolItems 渲染的 onClick，将 `onClick={() => item.path && navigateTo(item.path)}` 替换为：

```typescript
onClick={() => {
  if (item.action === 'privacy') {
    Taro.showModal({
      title: '隐私政策',
      content: '邑夏果酒小程序重视您的隐私保护。我们仅收集必要信息（昵称、学校、年龄）用于订单配送和年龄验证，不会向第三方分享您的个人信息。您可随时在"设置"中删除账户数据。',
      showCancel: false,
      confirmText: '我知道了'
    })
  } else if (item.path) {
    navigateTo(item.path)
  }
}}
```

## 3. 未成年人禁止饮酒警示语

**验证**：打开 `src/pages/product/index.tsx`，搜索 `未成年人`。
- 无"未成年人禁止饮酒" → ❌ 确认存在
- 有 → ✅

**修复**：

A. 在详情页商品描述末尾（`activeTab === 'detail'` 分支内，产品描述 `</Text>` 之后）添加：

```tsx
{product?.isAlcohol && (
  <View className="mt-4 p-3 bg-amber-50 rounded-lg border border-amber-200">
    <View className="flex items-center gap-1 mb-1">
      <CircleAlert size={14} color="#D97706" />
      <Text className="text-xs font-medium text-amber-700">温馨提示</Text>
    </View>
    <Text className="text-xs text-amber-600 leading-4">未成年人禁止饮酒 · 孕妇及饮酒后请勿驾车 · 适量饮酒，健康生活</Text>
  </View>
)}
```

B. 将底部操作栏 `<Text>购买果酒需年满18周岁</Text>` 替换为 `<Text>未成年人禁止饮酒 · 购买需年满18周岁</Text>`

## 4. 女性精灵角色未下架

**验证**：打开 `src/mock/sprites.ts`，检查精灵数据。
- 包含欣欣、霏霏、清爽精灵等女性角色 → ❌ 确认存在
- 只剩藏府君 → ✅

**修复**：将精灵数据替换为只保留藏府君。如果文件中有导出数组，整段替换为只含藏府君的版本。藏府君数据：

```typescript
{
  id: 'sprite_cangfujun',
  name: '藏府君',
  title: '养生守护者',
  description: '"五脏六腑，各司其职。我是藏府君，守护你的身体健康。每一款邑夏果酒，都蕴含着中医养生的智慧。"',
  image: '/assets/images/spirits/cangfujun.png',
  productId: 'prod_pomegranate_new',
  productName: '榴红心事',
  color: '#8B5CF6',
}
```

如果 `cangfujun.png` 不存在，image 先设为空字符串 `''`。同时检查 `src/pages/sprites/index.tsx` 是否也硬编码了精灵数据，如果有也一并修改。

## 5. console.log 清理

**验证**：全局搜索 `console.log`，统计数量。
- 44处 → ❌ 建议清理

**修复**：删除所有 `console.log(...)` / `console.debug(...)` 语句，保留 `console.error` 不动。不修改其他逻辑。

---

**执行完毕后，输出每个问题的验证结果和修复状态汇总表。**

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。

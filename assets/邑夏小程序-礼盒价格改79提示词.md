# 邑夏小程序 - 仅修改礼盒价格为¥79

## ⚠️ 铁律：不要用AI生成任何图片！不要修改/删除已有图片文件！只改代码！

---

## 任务目标

将两款礼盒的价格从当前值统一修改为 **¥79**。

---

## 修改文件：src/mock/products.ts

### 1. 缤纷礼盒（gift_juice_box）

找到 `id: 'gift_juice_box'` 的产品，修改以下字段：

- `price: 79`（不管现在是多少，改为79）
- `originalPrice: 79.2`
- specs中：`price: 79, originalPrice: 79.2`

### 2. 果酒混合礼盒（gift_wine_box）

找到 `id: 'gift_wine_box'` 的产品，修改以下字段：

- `price: 79`（不管现在是多少，改为79）
- `originalPrice: 75.2`
- specs中：`price: 79, originalPrice: 75.2`

---

## 验证

```bash
grep -A5 "gift_juice_box" src/mock/products.ts | grep "price"
# 必须看到 price: 79

grep -A5 "gift_wine_box" src/mock/products.ts | grep "price"
# 必须看到 price: 79

pnpm build:weapp
git add -A && git commit -m "fix: 礼盒价格统一为79元" && git push
```

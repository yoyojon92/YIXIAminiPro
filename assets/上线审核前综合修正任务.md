---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 7629647366345376000-data_volume/files/所有对话/主对话/上线审核前综合修正任务.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 4496376016153680#1780469894161
    ReservedCode2: ""
---
# 邑夏小程序 - 上线审核前综合修正任务

## 目标
将GitHub仓库代码做一次性综合修正，修正完成后用户git pull + pnpm build:weapp即可上传审核。解决：①主包37MB超限 ②图片加载500错误 ③V2.0功能未生效 ④代码质量扫描不通过。

## 仓库信息
- **GitHub仓库**：https://github.com/yoyojon92/YIXIAminiPro
- **分支**：main
- **当前最新commit**：3d428df（用户本地推送的V2.0改进）

## 执行步骤

### 第1步：Clone仓库
```bash
git clone https://github.com/yoyojon92/YIXIAminiPro.git
cd YIXIAminiPro
```

### 第2步：图片路径全部改CDN（解决37MB超限+500错误）

**核心原则：所有产品图/IP图/Banner用CDN，只有tabbar图标保留本地。**

CDN BASE URL：`https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images`

用sed批量替换以下7个文件：

```bash
# 1. products.ts - 产品图
sed -i "s|'/assets/images/products/|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/products/|g" src/mock/products.ts

# 2. organLords.ts - 器官大人IP图
sed -i "s|'/assets/images/organ-lords/|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/organ-lords/|g" src/data/organLords.ts

# 3. index.tsx - 首页
sed -i "s|'/assets/images/products/|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/products/|g" src/pages/index/index.tsx
sed -i "s|'/assets/images/organ-lords/|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/organ-lords/|g" src/pages/index/index.tsx

# 4. ipCharacters.ts - IP角色图
sed -i "s|'/assets/images/ip/|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/ip/|g" src/data/ipCharacters.ts

# 5. sprites页面
sed -i "s|'/assets/images/spirits/|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/spirits/|g" src/pagesSocial/sprites/index.tsx

# 6. mock sprites - Banner图
sed -i "s|'./assets/images/banner|'https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/banner|g" src/mock/sprites.ts
```

**验证**：替换完后执行，应无输出：
```bash
grep -rn "'/assets/images/" src/ --include="*.ts" --include="*.tsx" | grep -v "cdn.jsdelivr" | grep -v tabbar
```

### 第3步：修改config/index.ts - 去掉产品图片copy规则

找到 `copy.patterns` 部分，改成只保留tabbar：

```typescript
    copy: {
      patterns: [
        // tabbar图标（小程序tabbar必须使用本地文件）
        { from: 'src/assets/tabbar/', to: 'dist/assets/tabbar/' },
      ],
      options: {},
    },
```

删除所有products/organ-lords相关的copy行。

### 第4步：修改project.config.json - 代码质量扫描

把 `setting` 改为：
```json
{
  "miniprogramRoot": "./dist",
  "cloudfunctionRoot": "./cloudfunctions/",
  "projectname": "yixia-mini-program",
  "description": "邑夏果酒果汁小程序 - 基于微信云开发",
  "appid": "wxd006de892f57a2f5",
  "setting": {
    "urlCheck": true,
    "es6": false,
    "enhance": false,
    "compileHotReLoad": false,
    "postcss": false,
    "minified": true,
    "lazyCodeLoading": "requiredComponents"
  },
  "compileType": "miniprogram",
  "packOptions": {
    "ignore": []
  }
}
```

**关键**：`miniprogramRoot` 必须是 `"./dist"`，`minified` 改为 `true`，新增 `lazyCodeLoading`。

### 第5步：首页Banner改CDN

在 `src/pages/index/index.tsx` 中，找到 banner-slogan.png 的引用：
```
https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@6911dae/src/assets/images/banner-slogan.png
```
改commit hash为3d428df：
```
https://cdn.jsdelivr.net/gh/yoyojon92/YIXIAminiPro@3d428df/src/assets/images/banner-slogan.png
```

### 第6步：确认V2.0代码已包含

验证以下文件存在且包含V2.0功能：
- `src/store/memberStore.ts` — 9.9创始会员体系
- `src/utils/discount.ts` — 满减计算
- `src/components/ticket-selector/index.tsx` — 小酒票弹窗
- `src/components/member-modal/index.tsx` — 会员权益展示
- `src/pages/cart/index.tsx` — 满减提示+小酒票加购+自提简化

如果commit 3d428df已包含这些文件，则无需额外操作。

### 第7步：Git提交推送

```bash
git add -A
git commit -m "fix: 图片CDN化+代码质量扫描修复-准备审核上传"
git push origin main
```

## ⚠️ 关键约束（不可违反）

1. **`'/assets/tabbar/` 不要改** — tabbar图标必须本地
2. **CDN hash用 `@3d428df`** — 当前最新commit
3. **`miniprogramRoot` 必须是 `"./dist"`** — Taro编译产物在dist目录
4. **git push后立即清除credentials** — PAT安全
5. **不要动图片文件本身** — 只改引用路径
6. **不要修改src/store/memberStore.ts等V2.0文件** — 已经在仓库里了

## 修正后预期效果
- dist目录 ~1MB（不含产品图片）
- 代码质量扫描全通过（JS压缩✅ + 组件按需注入✅）
- 产品图通过CDN加载，不会500
- V2.0功能（会员+满减+小酒票+购物车优化）正常工作

## 用户操作流程
修正完成后用户只需：
```bash
cd d:\Users\jonwa\Desktop\YIXIAminiPro
git pull origin main
pnpm build:weapp
```
然后微信开发者工具编译 → 点上传 → 提交审核

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
